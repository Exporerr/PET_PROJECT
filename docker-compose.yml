version: "3.9"

services:

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: api-service
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      API_ENV: /app/internal/api-service/Configs/.env
      DB_SERVICE_URL: http://db-service:8081
    networks:
      - appnet

  db:
    build:
      context: .
      dockerfile: Dockerfile.db
    container_name: db-service
    ports:
      - "8081:8081"
    depends_on:
      - postgres
    environment:
      DB_ENV: /app/internal/db-service/configs/.env
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: post4302
      DB_NAME: taskss
    networks:
      - appnet

  postgres:
    image: postgres:16.11-bookworm
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: post4302
      POSTGRES_DB: taskss
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - appnet
  migrate:
  build:
    context: .
    dockerfile: Dockerfile.db
  depends_on:
    - db
  environment:
    DB_HOST: postgres
    DB_PORT: 5432
    DB_USER: postgres
    DB_PASSWORD: post4302
    DB_NAME: taskss
  command: ["migrate", "-path", "/app/migrations", "-database", "postgres://postgres:post4302@postgres:5432/taskss?sslmode=disable", "up"]
  restart: "no"
  networks:
    - appnet
  kafka:
  image:

 


volumes:
  pgdata:

networks:
  appnet:

